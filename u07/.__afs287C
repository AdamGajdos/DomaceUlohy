#!/bin/sh
vstup= readlink -f $1
vystup= readlink -f $2
git add --patch ccr.patch
git diff --staged
diff --git a/"$vstup" b/"$vstup" > "$vystup"
csplit -f subor $vystup diff -u -r a/"$vystup" b/"$vystup"
